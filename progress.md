# Progress

## Beurt 1 — setup (klaar)
- Astro 5-scaffold in `./site` (pnpm, TS strict, @astrojs/sitemap, Fontsource Inter + Space Grotesk, GSAP + Lenis). Tokens uit `context/design-tokens.json` staan in `site/src/styles/tokens.css`; basis/knoppen/formulier-states in `global.css`. BaseLayout (SEO/OG/JSON-LD/Plausible/skip-link) + Header (frosted, Diensten-dropdown, mobiel drawer met focus-trap) + Footer (ghost-wordmark, live open-status-pill) + gebrande 404 + robots.txt. `pnpm build` slaagt (ook via root-package.json proxy).
- Volgende beurt (page_home): `src/pages/index.astro` is een MINIMALE tijdelijke hero — volledig vervangen volgens `pages/home.md`. Reveals: zet `data-reveal` / `data-reveal-group` op secties (motion.ts pakt ze op); count-up via `data-count-to`/`data-count-decimals`/`data-count-suffix`. Header-CTA is bewust secundair gestyled zodat de pagina-hero de enige primaire CTA per viewport heeft.
- Let op: systeem-Node is v24 (tech-stack zegt 22.x; nvm-switch kan niet in deze sandbox, Astro 5 draait prima op 24). pnpm 11: build-scripts esbuild/sharp goedgekeurd via `site/pnpm-workspace.yaml` (`allowBuilds`). Tijdelijk domein `stucq.pages.dev` — zie open-questions.md. JS-bundel ~50,6 kB gzip (GSAP+ScrollTrigger+Lenis), randje van 50 kB-per-route-budget — bewaken bij qa.

## Beurt 2+3 — page_home (klaar)
- Beurt 2 bouwde de volledige homepage (alle 9 blueprint-secties in `site/src/pages/index.astro`: hero-split met voor-na beeld + sweep-signature, stat-band met count-up, probleem/oplossing-contrast, beeld-gedreven dienstkaarten, voor-na toggle-galerij, 4-stappen werkwijze, garantie-iconen, review-scoreblok zonder verzonnen citaten, CTA-band met sfeerbeeld + scrim) maar crashte vóór administratie. Beurt 3 verifieerde alles: `pnpm build` slaagt, geen TODO/lorem, geen verboden zinsneden, consequent "u", geen uitroeptekens, micro-garantie bij beide CTA's, demo-beelden via astro:assets met `data-demo="true"` — daarna `passes: true` gezet.
- Hero-beelden voor home staan al in `site/src/assets/images/` (beurt 2 kopieerde ze uit public/images). Volgende pagina's: zelfde aanpak, beelden importeren vanuit `site/src/assets/images/` (kopieer ontbrekende slots uit `public/images/` volgens README-mapping).
- Screenshots niet mogelijk in deze sandbox (geen Playwright/Chrome-MCP; paden buiten projectmap geblokkeerd) — visuele check gedaan via statische CSS-review (mobile-first grids, geen 100vw/vaste px-breedtes). Bij qa-beurt alsnog visueel testen als tooling beschikbaar is.
- Volgende beurt (page_offerte): hergebruik header/footer + sectiepatronen van home; formulier (naam/telefoon/e-mail/type werk/omschrijving/postcode) post naar Cloudflare Pages Function — die bestaat nog niet, aanmaken onder `site/functions/` of als Astro endpoint.

## Beurt 4 — page_offerte (klaar)
- `site/src/pages/offerte.astro` gebouwd: alle 9 blueprint-secties (kop met 3 garantie-chips, bewijs-strip, formulier-card + sticky garantieblok, bel-band, dienst-icoonkaarten, 4-stappen traject, voor-na duo's met Voor/Na-chips, review-scoreblok, donkere CTA-band die ankert naar `#offerte-formulier`). Formulier post naar nieuwe Pages Function `site/functions/api/offerte.ts` (honeypot `faxnummer`, JSON-pad voor fetch, 303-redirect naar `#aanvraag-ontvangen`/`#aanvraag-mislukt` als no-JS-fallback via CSS `:target`). Validatie/laad/succes/fout-strings exact uit microcopy.md; lead-aflevering (e-mail/CRM) nog niet geconfigureerd — zie open-questions.md.
- Bewuste keuzes: formulierlabels neutraal-formeel ("Naam", niet "Jouw naam") omdat de microcopy-bibliotheek jij-vorm botst met de AC "uitsluitend u"; geen Google-profiel-link en geen keurmerk-slot (URL/keurmerken onbevestigd, zie open-questions.md); reviewsectie zonder verzonnen citaten/namen (zelfde patroon als home). 2 primaire CTA's in gescheiden viewports (submit + CTA-band), header-CTA blijft secundair.
- Volgende beurt (page_projecten): `prompts/build-projecten.md` bestaat al; hero-beeld `hero-projecten.jpg` + `drone-gevelwerk.jpg` (video-slot: alleen still als poster, géén echte video beschikbaar) moeten nog uit `public/images/` naar `site/src/assets/images/` gekopieerd worden. Voor-na toggle-patroon (`data-ba`) staat in index.astro, duo-patroon in offerte.astro.

## Beurt 5 — page_projecten (klaar)
- `site/src/pages/projecten.astro` gebouwd: alle 9 blueprint-secties (full-bleed beeld-hero met scrim + sweep-signature, kerncijfer-band met icoonblokken, filterbare voor-na galerij met dienst-filterpills + data-ba toggles, drone-blok met feiten-dl, beeld-gedreven dienstkaarten, review-scoreblok, donkere trackrecord statement-band, keurmerken-band zonder nep-logo's, CTA-band met cta-sfeer beeld). `pnpm build` slaagt; checks schoon (geen verboden zinsneden, geen uitroeptekens, u-vorm, 1× btn--primary).
- Bewuste keuzes: géén jaartallen/oppervlak/oplevermaand op kaarten (demo-README verbiedt verzonnen opleverdata — blueprint vroeg erom maar dat zou een verzonnen feit zijn); drone-slot als still met chip "Still uit drone-opname" (geen echte video, zie open-questions); galerij = 3 echte voor-na paren, niet kunstmatig opgevuld tot 2×3; geen Google-profiel-link (URL onbevestigd). hero-projecten.jpg + drone-gevelwerk.jpg gekopieerd naar `site/src/assets/images/`.
- Volgende beurt (page_gevelwerk): `prompts/build-gevelwerk.md` + `pages/gevelwerk.md` lezen; hero-gevelwerk.jpg moet nog uit `public/images/hero/` gekopieerd worden; dienstkaart/galerij/CTA-patronen staan in index.astro en projecten.astro. Links naar /gevelwerk/ bestaan al vanaf home + projecten.

## Beurt 6 — page_gevelwerk (klaar)
- `site/src/pages/gevelwerk.astro` gebouwd: compacte beeld-hero (hero-gevelwerk.jpg voor-na split, sweep-signature, primaire CTA + micro-garantie boven de vouw), kern-inhoud (genummerde werklijst naast dienst-gevelwerk.jpg + 3-stappen vaste-prijs-rij), bewijsblok (voor-na toggle gevel + feitenlijst + secundaire link naar /projecten/ + drone-still 21:9 met chip), CTA-band met cta-sfeer. `pnpm build` slaagt (5 pagina's); checks schoon (geen verboden zinsneden, u-vorm, geen uitroeptekens, 2× btn--primary in gescheiden viewports zoals home).
- hero-gevelwerk.jpg gekopieerd naar `site/src/assets/images/hero/`. Geen link naar /werkwijze/ opgenomen (pagina bestaat nog niet; bewust één beslispad offerte/bellen).
- Volgende beurt (page_binnenafwerking): let op de strengere AC (9 secties vereist, micro-garantie bij elke CTA). hero-binnenafwerking.jpg moet nog uit `public/images/hero/` gekopieerd worden; herbruik page-hero/werklijst/stappen/ba-card-patronen uit gevelwerk.astro, galerij-binnen-voor/na staan al in site-assets.

## Beurt 7 — page_binnenafwerking (klaar)
- `site/src/pages/binnenafwerking.astro` gebouwd: alle 9 blueprint-secties (split-hero met garantie-badge + sweep-signature, probleem/oplossing-band, 6 icoonkaarten dienstaanbod, voor-na toggle + "tijdens"-werkfoto met secundaire link naar /projecten/, review-scoreblok zonder verzonnen citaten, 4-stappen werkwijze, werkgebied-band met plaats-pills + contactkaart, 3 garantie-iconen + keurmerk-noot zonder nep-logo's, CTA-band met cta-sfeer). `pnpm build` slaagt (6 pagina's); checks schoon (geen verboden zinsneden/TODO/jij-vorm/uitroeptekens, 1× h1, 5× data-demo, 2× btn--primary in gescheiden viewports). hero-binnenafwerking.jpg gekopieerd naar site-assets.
- Bewuste keuzes: galerij-locatielabels generiek ("Haarlem", "regio Haarlem") — geen verzonnen wijken/jaartallen (README verbiedt dat, blueprint-voorbeeld "Haarlem-Noord, 2024" dus genegeerd); geen Google-profiel-link (URL onbevestigd); op mobiel content vóór beeld zodat de primaire CTA boven de vouw staat, op desktop beeld links per blueprint (CSS order).
- Volgende beurt (page_microcement): `prompts/build-microcement.md` + `pages/microcement.md` lezen; hero-microcement.jpg moet nog uit `public/images/hero/` naar `site/src/assets/images/hero/`; dienst-microcement.jpg + galerij-vloer-voor/na staan al in site-assets. Patronen: binnenafwerking.astro (dienstpagina-opbouw) en gevelwerk.astro.

## Beurt 8 — page_microcement (klaar)
- `site/src/pages/microcement.astro` gebouwd volgens het generieke 4-secties-blueprint: beeld-hero (hero-microcement.jpg resultaat-shot, sweep-signature, primaire CTA + micro-garantie boven de vouw), toepassingen-werklijst naast dienst-microcement.jpg, "Kosten & doorlooptijd"-vragenblok (4 Q&A-icoonkaarten — blueprint vroeg expliciet om kosten/doorlooptijd-vragen; géén bedragen of doorlooptijden verzonnen, vaste-prijs-na-inspectie als enige prijspad), voor-na toggle vloer + feitenlijst + link /projecten/, CTA-band met cta-sfeer. `pnpm build` slaagt (7 pagina's); checks schoon (geen verboden zinsneden/jij-vorm/uitroeptekens, 1× h1, 5× data-demo, 2× btn--primary in bron = 3 in dist incl. drawer-CTA, gelijk aan alle andere pagina's).
- hero-microcement.jpg gekopieerd naar `site/src/assets/images/hero/`. Geen drone-blok (niet relevant voor vloeren); productfeiten beperkt tot algemene microcement-eigenschappen (naadloos, gelaagd aanbrengen, vloerverwarming-geschikt).
- Volgende beurt (page_werkwijze): `prompts/build-werkwijze.md` + `pages/werkwijze.md` lezen. Stappen-patroon staat in gevelwerk.astro (3 kaarten) en index.astro/offerte.astro (4 stappen); er is géén eigen hero-beeld voor werkwijze in public/images — overweeg tekstkop (mag per house-regels) of hergebruik over-werkomgeving.jpg (let op: die is gereserveerd voor over-pagina; tekstkop is veiliger).

## Beurt 9 — page_werkwijze (klaar)
- `site/src/pages/werkwijze.astro` gebouwd: tekst-hero met rustige primary-gloed (geen eigen werkwijze-beeld in public/images — tekstkop mag per house-regels; over-werkomgeving.jpg bewust bewaard voor over-pagina), verticale 5-stappen-tijdlijn met verbindingslijn + "Afspraak"-pill per stap (opname → offerte → planning → uitvoering → oplevering, sticky intro-kolom op desktop), bewijsblok ("vast op papier"-lijst + review-scoreblok 4,8/25+ + link /projecten/), CTA-band met cta-sfeer. `pnpm build` slaagt (8 pagina's); checks schoon (geen verboden zinsneden/jij-vorm/uitroeptekens/TODO, 1× h1, alleen tokens, 3× btn--primary in dist = gelijk aan andere pagina's).
- Bewuste keuzes: geen doorlooptijden of termijnen per stap verzonnen (alleen bevestigde feiten: 48u reactie, vaste prijs, schriftelijke planning, meerwerk na akkoord); OG-image = cta-sfeer.jpg; stappenpill-label "Afspraak" consistent over alle 5 stappen.
- Volgende beurt (page_over-stucq): `prompts/build-over-stucq.md` + `pages/over-stucq.md` lezen; hero-over-stucq.jpg én over-werkomgeving.jpg moeten nog uit `public/images/` naar `site/src/assets/images/` gekopieerd worden. Let op AC: werkgebied expliciet "Haarlem, Bloemendaal, Zandvoort en omgeving tot 15 km".

## Beurt 10 — page_over-stucq (klaar)
- `site/src/pages/over-stucq.astro` gebouwd: alle 9 blueprint-secties (split-hero met portret + kerngegevens-dl + sweep-signature, kerncijfers-band met 4 count-up stats incl. "15 km", achtergrond-tekst-beeld-blok met over-werkomgeving.jpg + link /werkwijze/, werkgebied-blok met schematische SVG-radiuskaart (15 km-ring, Haarlem/Bloemendaal/Zandvoort, caption "geen exacte kaart") + plaats-pills, review-scoreblok zonder citaten, waarborg-icoonkaarten + keurmerk-noot zonder nep-logo's, voor-na teaser met 2 toggle-koppels (gevel + binnen), contactblok met 4 icoonkaarten (geen formulier), CTA-band met cta-sfeer). `pnpm build` slaagt (9 pagina's); checks schoon (geen verboden zinsneden/jij-vorm/uitroeptekens/TODO, 1× h1, 7× data-demo, 2× micro-garantie, 3× btn--primary in dist = gelijk aan andere pagina's).
- Bewuste keuzes: géén statische kaart-afbeelding of maps-embed (geen echte kaartdata/API; schematische SVG met expliciete disclaimer is feitelijk veilig); geen Google-profiel-link (URL onbevestigd, copy verwijst tekstueel naar Google); hero-over-stucq.jpg + over-werkomgeving.jpg gekopieerd naar site-assets.
- Volgende beurt (page_faq): `prompts/build-faq.md` + `pages/faq.md` lezen. Geen eigen faq-hero-beeld in public/images — tekstkop zoals werkwijze.astro (rustige primary-gloed) is het patroon. Let op: FAQ-antwoorden alleen op bevestigde feiten (vaste prijs, 48u, werkgebied, garantie); geen doorlooptijden/prijzen verzinnen. Overweeg FAQPage JSON-LD (tech-constraints vraagt structured data per pagina).

## Beurt 11 — page_faq (klaar)
- `site/src/pages/faq.astro` gebouwd: tekst-hero (werkwijze-patroon, primaire CTA + micro-garantie boven de vouw), FAQ-sectie met sticky intro-kolom + thema-ankerlinks en 4 groepen × 14 vragen als native `<details name="…">`-accordion (exclusief-open per groep, hairline-dividers, 44px summaries, plus-icoon roteert bij open), CTA-band "Nog een vraag?" met tel + mail + primaire CTA. FAQPage JSON-LD (14 Questions) via `slot="head"` naast de LocalBusiness uit BaseLayout. `pnpm build` slaagt (10 pagina's); checks schoon (geen verboden zinsneden/jij-vorm/uitroeptekens/TODO/hex, 1× h1, 2× micro-garantie, 3× btn--primary in dist = gelijk aan andere pagina's).
- Bewuste keuzes: groepen "bezwaren eerst" (kosten → planning → werkgebied → garantie); antwoorden uitsluitend bevestigde feiten (vaste prijs, 48u, 50+ projecten sinds 2020, 4,8/25+ reviews, 15 km-werkgebied, KvK-geregistreerd/verzekerd — elders al gebruikt); géén doorlooptijden of tarieven verzonnen ("verschilt per type werk; standaardtermijnen noemen wij niet"); "Nog een vraag?"-blok routeert naar tel/mail i.p.v. /contact/ (pagina bestaat nog niet, zelfde regel als beurt 6); JSON-LD-antwoorden zijn de platte tekst zonder de losse doorkliklinks.
- Volgende beurt (page_contact): `prompts/build-contact.md` + `pages/contact.md` lezen. Formulier-patroon + Pages Function staan in offerte.astro / `site/functions/api/offerte.ts` (hergebruiken of aparte contact-function); contact-icoonkaarten-patroon staat in over-stucq.astro; AC eist klikbaar 0681943404, micro-garantie bij formulier én contactkaart, werkgebied "tot 15 km". Daarna /contact/-link overwegen in faq-CTA-band ("Nog een vraag?") en evt. footer/drawer checken.

## Beurt 12 — page_contact (klaar)
- `site/src/pages/contact.astro` gebouwd: alle 7 blueprint-secties (aardetint paginaheader met dominant klikbaar telefoonnummer naast de titel, bel/mail card-duo met micro-garantie eronder, contactformulier (naam/telefoon/e-mail/omschrijving) naar nieuwe Pages Function `site/functions/api/contact.ts` (honeypot + JSON-pad + 303 naar `#bericht-ontvangen`/`#bericht-mislukt`), werkgebied-blok met schematische SVG-radiuskaart (hergebruik over-stucq-patroon) + plaats-pills, openingstijden-tabel per dag met gemarkeerde spoed-noot, donkere vertrouwensband (4,8★ / 50+ projecten / vaste prijs), lichte CTA-band met primair "Offerte aanvragen" + secundair "Bekijk afgeronde projecten"). `pnpm build` slaagt (11 pagina's); checks schoon (geen verboden zinsneden/jij-vorm/TODO/hex, 1× h1, 7× tel-link, 3× micro-garantie, 3× btn--primary in dist = gelijk aan andere pagina's; "Bedankt!" is verplichte microcopy).
- Bewuste keuzes: géén primaire "Offerte aanvragen"-knop boven de vouw — blueprint sectie 1 maakt het telefoonnummer het dominante element (intent = directe bereikbaarheid), primaire CTA staat in de afsluitende band; weekend "Gesloten" afgeleid uit contact.json (ma–vr 7:00–17:00); CTA-band bewust vlak aardetint (AC: geen full-bleed sfeeroverload), vertrouwensband donker per blueprint. Header/footer-links naar /contact/ werken nu (bestonden al).
- Volgende beurt (demo-beelden): `public/images/README.md` slot-mapping naast de huidige imports leggen — meeste slots zijn al gekoppeld via astro:assets met data-demo="true" tijdens de paginabeurten; controleer vooral ontbrekende slots (logo? og-fallback?) en of álle beelden uit de README gebruikt worden waar bedoeld. Contact-pagina gebruikt bewust geen foto (alleen SVG-kaart + iconen).

## Beurt 13 — demo-beelden (klaar)
- Volledige audit van alle beeld-slots tegen `public/images/README.md`: alle 18 demo-beelden zijn gekoppeld op de bedoelde plekken (6 hero-splits op de juiste pagina's, dienst-trio op home/projecten/dienstpagina's, 3 galerij-voor/na-paren in de toggles, drone-still op projecten+gevelwerk, over-werkomgeving op over-pagina, cta-sfeer in CTA-banden). Dist-audit via Node-script: 38 content-imgs, allemaal `<picture>` met AVIF+WebP, expliciete width/height, `data-demo="true"`, hero's `fetchpriority="high"` (niet lazy), rest `loading="lazy" decoding="async"` — 0 problemen. `heroHome` in offerte.astro is alleen OG-fallback (toegestaan per asset-spec); geen kale `/images/`-verwijzingen in src; README-DEMO-waarschuwing onaangetast. Geen codewijzigingen nodig — taak was incrementeel al goed uitgevoerd tijdens de paginabeurten; deze beurt verifieerde en vinkte af.
- Volgende beurt (qa): eindcontrole hele site — pnpm build, 1 primaire CTA per pagina (let op: contact heeft primaire CTA onderaan, bewuste keuze beurt 12), Lighthouse mobile >= 90 (JS-bundel ~50 kB gzip bewaken, zie beurt 1), alle [CONFIRM:]-markers verzamelen in open-questions.md, prefers-reduced-motion check (motion.ts + tokens.css reduceMotion). Screenshots/Lighthouse alleen als tooling in sandbox beschikbaar is — anders statisch verifiëren en dat noteren.

## Beurt 14 — qa (klaar, ALLE taken passes:true)
- Eindcontrole hele site: `pnpm build` slaagt (11 pagina's, 0 fouten). CTA-audit per pagina via dist + bron: overal max 2× btn--primary in de body in gescheiden viewports (begin + afsluitende CTA-band; projecten zelfs 1, contact bewust alleen onderaan) + 1 verborgen drawer-CTA — conform "max 1 primaire CTA per viewport". Alle pagina's 1× h1, 0 kapotte interne links (Node-audit over dist), geen TODO/PLACEHOLDER/lorem in src. prefers-reduced-motion: drielaags gedekt (global.css kill-switch + `html.js [data-reveal]`-override, motion.ts `showEverything()`-gate die Lenis/GSAP volledig overslaat, Header-drawer eigen reduced-pad).
- Lighthouse kon niet draaien (geen Chrome/Playwright in sandbox; paden buiten projectmap geblokkeerd) — statisch geverifieerd en als open punt genoteerd: JS 49,3 kB gz (≤50 kB route-budget), hero-AVIF 480w 12 kB + preload/fetchpriority=high, alle scripts module/defer, font-preloads self-hosted, expliciete beeldmaten. Na deploy alsnog meten (commando staat in open-questions.md).
- open-questions.md herschreven: beide [CONFIRM:]-markers uit asset-spec (logo SVG+PNG, definitieve OG-image) expliciet verzameld + nieuwe punten (KvK-nummer leeg in contact.json terwijl footer "KvK-geregistreerd" zegt — wettelijk verplicht op NL-site; keurmerken; demo-beelden vervangen vóór livegang; Lighthouse-nameting).
- Let op (lokaal, geen blocker): iCloud-sync maakt " 2"-duplicaatbestanden aan in `site/dist/` (gitignored, komt nooit in repo/deploy — Cloudflare Pages bouwt vanaf git). `rm -rf dist` werd in deze sessie geweigerd; bij hinder lokaal handmatig dist weggooien of de map uit iCloud-sync halen.

## Beurt 15 — polish-leesbaarheid (klaar)
- Oplossing per hero: (1) **microcement/gevelwerk page-hero + projecten project-hero**: gradient-scrim verzwaard — mobiel vlakkere sterke scrim (92%→78%@50%→66% bovenin, tekst beslaat daar bijna de hele hero), desktop gelaagd (verticaal 92→74→40→18% + horizontale laag 60→32→0% van links, want tekst staat linksonder; rechterbovenhoek blijft vrij voor beeldimpact — geen volvlak-blok). (2) **cta-band (8 pagina's: home, gevelwerk, binnenafwerking, microcement, projecten, werkwijze, faq, over-stucq)**: 100deg-gradient rechterkant 55%→72% op mobiel (tekst loopt daar tot de rand), 60% op desktop; `cta-band__micro` neutral-300→neutral-100. (3) **eyebrow--inverse** op de 3 beeld-hero's neutral-300→neutral-100. Chips (70% vlak) en pills (50% boven op scrim) gecontroleerd: al AA.
- Aantoonbaar via `site/scripts/contrast-audit.mjs` (nieuw): composit echte beeldpixels (sharp) met exact de CSS-gradients per tekstzone op 375 én 1280px; ALLE zones AA met marge (slechtste: eyebrow mobiel 5,07:1 bij eis 4,5; h1 projecten-desktop 4,05:1 bij eis 3). `pnpm build` slaagt (11 pagina's); alleen CSS gewijzigd, copy/markup/CTA's onaangetast. Screenshots blijven onmogelijk in sandbox (zie beurt 3/14) — numerieke audit is het bewijs.
- Volgende beurt (polish-stappen): home zegt "vier vaste stappen" (index.astro werkwijze-sectie), werkwijze-pagina heeft 5 stappen (opname→offerte→planning→uitvoering→oplevering) en is leidend. Check ook offerte.astro (4-stappen traject) en faq.astro op stappen-aantallen.

## Beurt 16 — polish-stappen (klaar)
- Home, offerte én binnenafwerking geharmoniseerd naar de 5 werkwijze-stappen met identieke titels (Opname op locatie → Offerte met vaste prijs → Planning op papier → Uitvoering volgens planning → Oplevering); kopjes/intro's nu "vijf vaste stappen", pin+calendar-iconen toegevoegd waar ze ontbraken, stappen-grids 4→5 kolommen op 1024px+. FAQ zei al "vijf vaste stappen" (ongewijzigd). Gevelwerk-sectie "Vóór de start op papier" bewust gelaten: 3 genummerde punten over de prijsafspraak, zichtbare copy noemt géén stappen-aantal (alleen HTML-comment hernoemd) — geen tegenstrijdigheid. Dist-audit: alleen "vijf (vaste) stappen" komt nog voor; home/werkwijze/offerte/binnenafwerking tonen exact dezelfde staptitels; btn--primary ongewijzigd (3 per pagina incl. drawer); geen verboden zinsneden/jij-vorm/TODO; `pnpm build` slaagt (11 pagina's).
- Subtiele fix meegenomen: binnenafwerking beloofde "Vaste offerte binnen 48 uur" — dat overstatete het bevestigde feit (reactie binnen 48 uur); nu "binnen 48 uur plannen wij een opname".
- Volgende beurt (polish-slugs): routes hernoemen naar context/sitemap.md (/offerte→/offerte-aanvragen, /faq→/veelgestelde-vragen, /gevelwerk→/diensten/gevelwerk-buitenafwerking, /binnenafwerking→/diensten/binnenafwerking-renovatie, /microcement→/diensten/microcement-vloeren) + alle interne links, canonicals, sitemap, Pages Functions-verwijzingen en Astro-redirects van de oude paden. Let op: links zitten ook in Header/Footer-componenten, FAQ-ankerlinks, offerte-formulier-action en JSON-LD.

## Beurt 17 — polish-slugs (klaar)
- Routes hernoemd naar context/sitemap.md: offerte.astro→offerte-aanvragen.astro, faq.astro→veelgestelde-vragen.astro, en de drie dienstpagina's naar `src/pages/diensten/{gevelwerk-buitenafwerking,binnenafwerking-renovatie,microcement-vloeren}.astro` (relative imports één niveau opgehoogd). Alle interne links bijgewerkt in 13 bestanden (Header incl. `isActive('/veelgestelde-vragen/')`, Footer, alle pagina's, 404) en `functions/api/offerte.ts` redirect nu naar `/offerte-aanvragen/#…`. Redirects van de 5 oude paden via Astro `redirects`-config in astro.config.mjs.
- Geverifieerd: `pnpm build` slaagt (11 pagina's + 5 redirect-pagina's); sitemap-0.xml bevat uitsluitend de 10 nieuwe paden; canonicals/og:url volgen automatisch uit Astro.url; nieuw `site/scripts/link-audit.mjs` → 436 interne verwijzingen, 0 kapot; `site/scripts/check-redirects.mjs` tegen `pnpm preview` → alle 5 oude paden geven 301 naar het juiste doel (dist-fallback heeft óók meta-refresh + noindex + canonical naar nieuw pad), alle 5 nieuwe routes 200 met echte pagina. Regressie-check: 1× h1 en 3× btn--primary per geraakte pagina (ongewijzigd). Beide audit-scripts blijven staan voor polish-qa.
- Volgende beurt (polish-koppen): hyphenation uit op alle headings (hyphens: manual, geen woord-splitsende overflow-wrap) + text-wrap: balance; check alle h1/h2/h3 op 375px en 1280px. De globale kop-regel staat vermoedelijk in `site/src/styles/global.css` (overflow-wrap/hyphens uit tech-constraints); let op dat de no-overflow-eis op 320–375px blijft staan. NB: dist bevat lokaal iCloud-" 2"-duplicaten (gitignored, niet in deploy) — bij dist-greps altijd `index 2.html` uitsluiten.

## Beurt 18 — polish-koppen (klaar)
- `global.css` h1–h4: `hyphens: auto` → `manual` en `overflow-wrap: break-word` → `normal` (`text-wrap: balance` stond er al) — geen markup/copy geraakt, dus geen regressie op CTA's/zinsneden mogelijk. NB: tech-constraints.md zegt nog "overflow-wrap:break-word + hyphens:auto" voor koppen; de polish-taak overrulet dat bewust, het audit-script hieronder is het vangnet tegen toekomstige overflow.
- Aantoonbaar via nieuw `site/scripts/heading-audit.mjs`: parset de échte Space Grotesk WOFF-metrics (zlib + cmap/hmtx) en meet álle 175 koppen uit dist op 320/375/1280px incl. letter-spacing −0.02em — elk kopwoord past op één regel (krapste: h1 "Stucadoorsbedrijf" 282/288px op 320px; keurmerken-h2 op /projecten/ naast 44px-icoon apart doorgerekend: 217/228px). `pnpm build` slaagt (11 pagina's), link-audit 0 kapot, dist-CSS bevat alleen nog `hyphens:manual`/`overflow-wrap:normal` op koppen (de ene `overflow-wrap:anywhere` is de e-maillink op contact, geen kop).
- Volgende beurt (polish-hero-crop): voor-na deellijn centreren op home/gevelwerk/binnenafwerking/projecten-hero's — 50/50 grid met losse galerij-*-voor/na-helften + dunne witte deellijn, óf aspect-bewuste object-position. Splits zitten in index.astro, diensten/gevelwerk-buitenafwerking.astro, diensten/binnenafwerking-renovatie.astro, projecten.astro. Na markup-wijziging heading-audit.mjs én contrast-audit.mjs opnieuw draaien (scrims liggen op die hero's).

## Beurt 19 — polish-hero-crop (klaar)
- Alle 4 voor-na hero's omgebouwd van composiet-beeld (deellijn schoof weg door object-fit crop) naar een 50/50-grid van de losse galerij-helften: `hero__split`/`page-hero__split`/`project-hero__split` (absolute inset 0, `grid-template-columns: 1fr 1fr`, `gap: var(--spacing-xs)` op witte ondergrond = dunne deellijn die per constructie op élk breakpoint exact gecentreerd is; beide helften eigen object-fit cover, dus nooit 90/10). Home + binnenafwerking (split-kaart): galerij-gevel- resp. galerij-binnen-paren met beschrijvende Vóór/Na-alts; gevelwerk + projecten (full-bleed bg, aria-hidden): galerij-gevel- resp. galerij-vloer-paren, alt="". Beide helften fetchpriority=high + eager + data-demo, 2 AVIF-preloads per pagina; composiet hero-*.jpg's blijven uitsluitend OG-image. `overflow:hidden` per picture zodat de hero-settle-zoom (scale 1.06) de deellijn niet overlapt.
- Geverifieerd: `pnpm build` slaagt (11 pagina's). `contrast-audit.mjs` uitgebreid met split-sampler (per helft eigen coverMap + witte deellijn als gegarandeerd-geraakte worst-case-band) — ALLE zones AA, slechtste h1 desktop 4,16:1 (was 4,05). heading-audit: geen afbreking/overflow; link-audit: 440 verwijzingen, 0 kapot. Regressie: 1× h1, btn--primary-tellingen ongewijzigd, geen verboden zinsneden, geen lazy in hero, composiet niet meer in body.
- Volgende beurt (polish-kaart-crop): dienstkaart-foto's croppen het hoofd van de vakman af — object-position bijstellen op de `dienst-card`-beelden (3 kaarten op home/index.astro + kaart-varianten op projecten.astro; dienstpagina's gebruiken dezelfde dienst-*.jpg's in kern-inhoud-secties, ook checken). Beelden: dienst-gevelwerk.jpg (vakman op steiger), dienst-binnenafwerking.jpg, dienst-microcement.jpg. Vermoedelijk volstaat iets als `object-position: 50% 20%` per beeld; verifieer numeriek met een sharp-crop-berekening (zelfde coverMap-aanpak als contrast-audit — geen browser in sandbox).

## Beurt 20 — polish-kaart-crop (klaar)
- Crop-anker per vakman-foto gezet als `object-position` = gezichtspositie in het beeld (gevelwerk `49% 30%`, binnenafwerking `38% 24%`, microcement `54% 21%`) op álle 5 cover-plekken: dienstkaart-trio op home + projecten (via nieuwe `dienst-card__media--{gevelwerk,binnen,vloer}`-modifiers uit het `foto`-veld in de diensten-array), de 4/5 `dienst__media` op gevelwerk- en microcement-pagina, en de 4/3 `werkfoto__media` op binnenafwerking. Plus `transform-origin: 50% 30%` op de kaart-imgs zodat ook de hover-zoom (scale 1.05) van onderaf cropt i.p.v. het hoofd te raken. Analyse vooraf: de 3:2-kaartbox cropt een 3:2-bron statisch niet — de echte crops zitten in de hover-zoom en de smallere boxen op de dienstpagina's; de ankers maken het hoofd nu op élke boxverhouding gegarandeerd zichtbaar.
- Aantoonbaar via nieuw `site/scripts/card-crop-audit.mjs`: rekent per gebruiksplek het zichtbare cover-venster uit (object-fit + object-position + hover-zoom, werkfoto-rek gebound op 1:1) en checkt de handmatig geannoteerde hoofd-bboxes op 375/768/1280 — alle 21 checks OK (krapste: binnenafwerking-werkfoto gerekt: hoofd x 31–42% in venster 12,7–79,3%). Regressie: `pnpm build` slaagt (11 pagina's), link-audit 440/0 kapot, heading-audit schoon, btn--primary/h1/data-demo-tellingen ongewijzigd; alleen CSS + één class-attribuut, copy onaangetast.
- Volgende beurt (polish-achtergronden): drie nieuwe achtergronden in `public/images/backgrounds/` (bg-stuc-licht.jpg → werkwijze- en FAQ-hero, bg-stuc-donker.jpg → minstens één donkere band, bg-troffel-sweep.jpg → sectie-accent zoals garanties) subtiel koppelen; zie public/images/README.md. Eisen: AA-contrast behouden (contrast-audit.mjs uitbreiden of hergebruiken), data-demo='true', lazy onder de vouw, geoptimaliseerd via astro:assets of gecomprimeerde CSS-background. Donkere kandidaten: trackrecord-band op projecten, vertrouwensband op contact, CTA-banden zijn al beeld-gedreven (cta-sfeer) — niet dubbelen.
