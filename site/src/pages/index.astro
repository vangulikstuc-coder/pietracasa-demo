---
import { Picture, Image, getImage } from 'astro:assets';
import BaseLayout from '../layouts/BaseLayout.astro';
import heroHome from '../assets/images/hero/hero-home.jpg';
import dienstGevelwerk from '../assets/images/sections/dienst-gevelwerk.jpg';
import dienstBinnenafwerking from '../assets/images/sections/dienst-binnenafwerking.jpg';
import dienstMicrocement from '../assets/images/sections/dienst-microcement.jpg';
import galerijGevelVoor from '../assets/images/sections/galerij-gevel-voor.jpg';
import galerijGevelNa from '../assets/images/sections/galerij-gevel-na.jpg';
import galerijBinnenVoor from '../assets/images/sections/galerij-binnen-voor.jpg';
import galerijBinnenNa from '../assets/images/sections/galerij-binnen-na.jpg';
import galerijVloerVoor from '../assets/images/sections/galerij-vloer-voor.jpg';
import galerijVloerNa from '../assets/images/sections/galerij-vloer-na.jpg';
import ctaSfeer from '../assets/images/sections/cta-sfeer.jpg';
import bgTroffelSweep from '../assets/images/backgrounds/bg-troffel-sweep.jpg';
import dienstBetonCire from '../assets/images/sections/dienst-beton-cire.jpg';
import dienstSpuitwerk from '../assets/images/sections/dienst-spuitwerk.jpg';
import detailGevel from '../assets/images/sections/detail-gevel.jpg';
import projectBadkamerVoor from '../assets/images/sections/project-badkamer-voor.jpg';
import projectBadkamerNa from '../assets/images/sections/project-badkamer-na.jpg';
import projectWoonkamerVoor from '../assets/images/sections/project-woonkamer-voor.jpg';
import projectWoonkamerNa from '../assets/images/sections/project-woonkamer-na.jpg';
import projectHerenhuisVoor from '../assets/images/sections/project-herenhuis-voor.jpg';
import projectHerenhuisNa from '../assets/images/sections/project-herenhuis-na.jpg';
import reviewFoto1 from '../assets/images/sections/review-1.jpg';
import reviewFoto2 from '../assets/images/sections/review-2.jpg';
import reviewFoto3 from '../assets/images/sections/review-3.jpg';

/* DEMO-reviews: verzonnen voorbeelden met AI-portretten (data-demo).
   Vóór livegang vervangen door echte Google-reviews — zie content-truth.md. */
const demoReviews = [
  {
    foto: reviewFoto1,
    naam: 'Marije van den Berg',
    plaats: 'Haarlem · binnenafwerking',
    quote:
      'Vooraf een vaste prijs op papier, en die klopte tot op de euro. De wanden zijn strakker dan we durfden hopen.',
  },
  {
    foto: reviewFoto2,
    naam: 'Henk de Wit',
    plaats: 'Heemstede · gevelwerk',
    quote:
      'Op de afgesproken maandag stond de steiger er, twee weken later was de gevel klaar. Precies zoals afgesproken.',
  },
  {
    foto: reviewFoto3,
    naam: 'Ruben Visser',
    plaats: 'Projectleider bouwbedrijf · regio Haarlem',
    quote:
      'Pietracasa draait inmiddels meerdere projecten met ons mee. Planning staat, kwaliteit constant – zeldzaam in de afbouw.',
  },
];
import fotoStap1 from '../assets/images/sections/stap-1-opname.jpg';
import fotoStap2 from '../assets/images/sections/stap-2-offerte.jpg';
import fotoStap3 from '../assets/images/sections/stap-3-planning.jpg';
import fotoStap4 from '../assets/images/sections/stap-4-uitvoering.jpg';
import fotoStap5 from '../assets/images/sections/stap-5-oplevering.jpg';

const stapFotos = [fotoStap1, fotoStap2, fotoStap3, fotoStap4, fotoStap5];
import detailWand from '../assets/images/sections/detail-wand.jpg';
import detailVloer from '../assets/images/sections/detail-vloer.jpg';

/* Hero als 50/50-split uit de losse voor/na-helften: de deellijn ligt daardoor
   op elk breakpoint exact op het midden (composiet hero-home.jpg is alleen nog OG-beeld) */
const heroHalfWidths = [320, 640, 960, 1248];
const heroHalfSizes = '(min-width: 1024px) 25vw, 50vw';
const heroPreloadSrcsets = await Promise.all(
  [galerijGevelVoor, galerijGevelNa].map(async (src) => {
    const images = await Promise.all(
      heroHalfWidths.map(async (width) => ({
        width,
        image: await getImage({ src, width, format: 'avif' }),
      }))
    );
    return images.map(({ width, image }) => `${image.src} ${width}w`).join(', ');
  })
);

const ogImage = await getImage({ src: heroHome, width: 1200, format: 'jpg' });

const cardWidths = [420, 840];
const cardSizes = '(min-width: 768px) 30vw, 100vw';

const diensten = [
  {
    href: '/diensten/gevelwerk-buitenafwerking/',
    titel: 'Gevelwerk & buitenafwerking',
    belofte: 'Strakke gevels op vaste prijs, binnen de afgesproken bouwtermijn.',
    src: dienstGevelwerk,
    alt: 'Vakman stukadoort een gevel vanaf een steiger',
    foto: 'gevelwerk',
  },
  {
    href: '/diensten/binnenafwerking-renovatie/',
    titel: 'Binnenafwerking & renovatie',
    belofte: 'Wanden en plafonds strak opgeleverd – klaar voor verf of behang.',
    src: dienstBinnenafwerking,
    alt: 'Vakman pleistert een binnenwand',
    foto: 'binnen',
  },
  {
    href: '/diensten/microcement-vloeren/',
    titel: 'Microcement vloeren',
    belofte: 'Naadloze vloeren met een vaste prijsopgave vooraf.',
    src: dienstMicrocement,
    alt: 'Vakman brengt microcement aan op een vloer',
    foto: 'vloer',
  },
  {
    href: '/diensten/beton-cire/',
    titel: 'Beton ciré',
    belofte: 'Waterbestendig en naadloos – badkamer, vloer en wand in één geheel.',
    src: dienstBetonCire,
    alt: 'Vakman brengt beton ciré aan op een badkamerwand',
    foto: 'betoncire',
  },
  {
    href: '/diensten/spuitwerk-latex/',
    titel: 'Spuitwerk · latex',
    belofte: 'Wanden en plafonds streeploos gespoten – sneller en strakker dan rollen.',
    src: dienstSpuitwerk,
    alt: 'Strak wit gespoten wanden en plafond in een lege woonkamer',
    foto: 'spuitwerk',
  },
];

const galerij = [
  {
    id: 'herenhuis',
    titel: 'Herenhuis bovenverdieping',
    label: 'Gevelwerk · Aerdenhout',
    meta: ['± 80 m²', '3 weken'],
    voor: { src: projectHerenhuisVoor, alt: 'Voor: herenhuis met gescheurd en afgebrokkeld stucwerk' },
    na: { src: projectHerenhuisNa, alt: 'Na: herenhuis met strak gestuukte verdieping' },
  },
  {
    id: 'jaren30',
    titel: 'Jaren-30 kamer in ere hersteld',
    label: 'Herstel + spuitwerk · Bloemendaal',
    meta: ['± 72 m²', '5 werkdagen'],
    voor: { src: projectWoonkamerVoor, alt: 'Voor: vergeelde kamer met scheuren en waterschade' },
    na: { src: projectWoonkamerNa, alt: 'Na: strak wit gespoten kamer met origineel ornament' },
  },
  {
    id: 'badkamer',
    titel: 'Badkamer zonder voegen',
    label: 'Beton ciré · Heemstede',
    meta: ['± 14 m²', '6 werkdagen'],
    voor: { src: projectBadkamerVoor, alt: 'Voor: gedateerde badkamer met beige tegels' },
    na: { src: projectBadkamerNa, alt: 'Na: badkamer volledig in warmgrijs beton ciré' },
  },
];

const stappen = [
  {
    nr: '01',
    titel: 'Opname op locatie',
    tekst:
      'U vraagt een offerte aan via het formulier of telefonisch. Binnen 48 uur plannen wij een opname op locatie.',
    icon: 'pin',
  },
  {
    nr: '02',
    titel: 'Offerte met vaste prijs',
    tekst: 'Na de opname ontvangt u een schriftelijke offerte: vaste prijs en heldere werkomschrijving.',
    icon: 'document',
  },
  {
    nr: '03',
    titel: 'Planning op papier',
    tekst: 'Start- en opleverdatum staan vóór de start zwart op wit. U weet wanneer het werk klaar is.',
    icon: 'calendar',
  },
  {
    nr: '04',
    titel: 'Uitvoering volgens planning',
    tekst: 'Eén vakman is uw aanspreekpunt. Meerwerk gebeurt alleen na uw schriftelijk akkoord.',
    icon: 'roller',
  },
  {
    nr: '05',
    titel: 'Oplevering',
    tekst: 'U loopt het werk samen met de vakman na. Oplevering op de afgesproken datum, binnen de vaste prijs.',
    icon: 'badge',
  },
];

const garanties = [
  {
    titel: 'Vaste prijsgarantie',
    tekst: 'De offerteprijs is de eindprijs. Meerwerk alleen na uw schriftelijk akkoord.',
    icon: 'euro',
  },
  {
    titel: 'Op tijd opgeleverd',
    tekst: '50+ projecten sinds 2020, allemaal binnen de afgesproken planning.',
    icon: 'clock',
  },
  {
    titel: 'Schriftelijke afspraken',
    tekst: 'Prijs, planning en oplevering staan vóór de start op papier.',
    icon: 'contract',
  },
  {
    titel: 'KvK-geregistreerd & verzekerd',
    tekst: 'Voor zowel particulier als zakelijk werk in de regio Haarlem.',
    icon: 'shield',
  },
];

const problemen = [
  'Onduidelijke kosten – de eindprijs blijkt hoger dan de offerte.',
  'Uitloop – de planning schuift zonder overleg.',
  'Niets op papier – afspraken zijn achteraf niet te controleren.',
];

const oplossingen = [
  'Schriftelijke vaste prijs vóór de start. Meerwerk alleen na uw akkoord.',
  'Planning vooraf vastgelegd. 50+ projecten sinds 2020 op tijd opgeleverd.',
  'Elke afspraak op papier, van offerte tot oplevering. Reactie binnen 48 uur.',
];

const starPath =
  'M12 2.8l2.85 5.78 6.38.93-4.62 4.5 1.09 6.36L12 17.37l-5.7 3 1.09-6.36-4.62-4.5 6.38-.93L12 2.8z';

const icons: Record<string, string> = {
  star: `<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="${starPath}"/></svg>`,
  starPartial: `<svg viewBox="0 0 24 24" aria-hidden="true"><defs><linearGradient id="star-partial-grad"><stop offset="0.8" stop-color="currentColor"/><stop offset="0.8" stop-color="transparent"/></linearGradient></defs><path d="${starPath}" fill="url(#star-partial-grad)" stroke="currentColor" stroke-width="1"/></svg>`,
  check:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4.5 12.5l5 5 10-11"/></svg>',
  x: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>',
  phone:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>',
  pin: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 21s-7-5.6-7-11a7 7 0 0 1 14 0c0 5.4-7 11-7 11z"/><circle cx="12" cy="10" r="2.5"/></svg>',
  calendar:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 11h18"/><path d="m9.5 15.5 2 2 3.5-4"/></svg>',
  document:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/><path d="M16 13H8M16 17H8M10 9H8"/></svg>',
  roller:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="3" width="13" height="6" rx="2"/><path d="M16 6h4v4h-8v4"/><path d="M12 14v6"/></svg>',
  badge:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="9" r="6"/><path d="m9.5 9 1.8 1.8L15 7.2"/><path d="M8.5 13.5 7 22l5-3 5 3-1.5-8.5"/></svg>',
  euro: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M17.5 7a6.5 6.5 0 1 0 0 10"/><path d="M5 10.5h8M5 13.5h8"/></svg>',
  clock:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.5 2"/></svg>',
  contract:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/><path d="m9 15 2 2 4-4"/></svg>',
  shield:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3l7 3v5c0 4.5-3 8-7 10-4-2-7-5.5-7-10V6z"/><path d="m9 11.5 2 2 4-4"/></svg>',
};
---

<BaseLayout
  title="Stucadoor in Haarlem | Pietracasa"
  description="Stucadoor in Haarlem voor gevelwerk, binnenafwerking en microcement. Vaste prijs, 50+ projecten op tijd opgeleverd. Vraag vrijblijvend een offerte aan."
  ogImage={ogImage.src}
>
  <link
    slot="head"
    rel="preload"
    as="image"
    type="image/avif"
    imagesrcset={heroPreloadSrcsets[0]}
    imagesizes={heroHalfSizes}
  />
  <link
    slot="head"
    rel="preload"
    as="image"
    type="image/avif"
    imagesrcset={heroPreloadSrcsets[1]}
    imagesizes={heroHalfSizes}
  />

  <!-- 1. Hero -->
  <section class="hero" aria-labelledby="hero-title">
    <div class="container hero__grid">
      <div class="hero__media">
        <div class="hero__ba" data-ba>
          <div class="ba-card__media hero__ba-media">
            <div class="ba-card__panel is-active" data-ba-panel="na">
              <Picture
                src={galerijGevelNa}
                widths={heroHalfWidths}
                sizes={heroHalfSizes}
                formats={['avif', 'webp']}
                alt="Na: dezelfde gevel strak gestuukt en geschilderd"
                fetchpriority="high"
                loading="eager"
                data-demo="true"
              />
            </div>
            <div class="ba-card__panel" data-ba-panel="voor" aria-hidden="true">
              <Picture
                src={galerijGevelVoor}
                widths={heroHalfWidths}
                sizes={heroHalfSizes}
                formats={['avif', 'webp']}
                alt="Vóór: gevel met gescheurd en afgebladderd stucwerk in Haarlem"
                fetchpriority="high"
                loading="eager"
                data-demo="true"
              />
            </div>
          </div>
        </div>
        <p class="hero__chip">Voor en na · gevelproject Haarlem</p>
      </div>
      <div class="hero__content" data-hero-entrance>
        <p class="eyebrow">Gevelwerk · Binnenafwerking · Microcement vloeren</p>
        <h1 id="hero-title">Stucadoor in Haarlem – <br class="br-mobiel" />vaste&nbsp;prijs, aantoonbaar op&nbsp;tijd</h1>
        <p class="hero__sub">
          U weet vooraf wat het kost – en Pietracasa houdt zich daaraan. 50+ projecten in Haarlem en
          omgeving sinds 2020, allemaal binnen budget en planning opgeleverd.
        </p>
        <ul class="hero__proof" role="list">
          <li>
            <span class="hero__proof-star" set:html={icons.star} aria-hidden="true" />
            4,8 op Google
          </li>
          <li>25+ geverifieerde reviews</li>
          <li>50+ projecten sinds 2020</li>
        </ul>
        <div class="hero__actions">
          <a href="/offerte-aanvragen/" class="btn btn--primary">
            Offerte aanvragen
            <span class="btn__arrow" aria-hidden="true">&rarr;</span>
          </a>
          <a href="/projecten/" class="btn btn--secondary">Bekijk afgeronde projecten</a>
        </div>
        <p class="micro">Vrijblijvend – vaste prijs, reactie binnen 48 uur.</p>
      </div>
    </div>
  </section>

<!-- 3. Probleem naar oplossing -->
  <section class="section section--alt" aria-labelledby="contrast-title">
    <div class="container">
      <header class="section-head" data-reveal>
        <p class="eyebrow">Probleem en oplossing</p>
        <h2 id="contrast-title">Geen onduidelijke kosten. Geen uitloop.</h2>
        <p class="section-head__intro">
          Onduidelijke kosten en uitloop zijn de twee redenen waarom opdrachtgevers afhaken. Pietracasa
          werkt daarom uitsluitend op vaste prijs en legt elke afspraak schriftelijk vast.
        </p>
      </header>
      <div class="contrast" data-reveal-group>
        <div class="contrast__col">
          <h3>Waar opdrachtgevers op afhaken</h3>
          <ul role="list">
            {
              problemen.map((tekst) => (
                <li>
                  <span class="mark mark--x" set:html={icons.x} aria-hidden="true" />
                  {tekst}
                </li>
              ))
            }
          </ul>
        </div>
        <div class="contrast__col contrast__col--oplossing">
          <h3>Zo lost Pietracasa dat op</h3>
          <ul role="list">
            {
              oplossingen.map((tekst) => (
                <li>
                  <span class="mark mark--check" set:html={icons.check} aria-hidden="true" />
                  {tekst}
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- 4. Diensten -->
  <section class="section" aria-labelledby="diensten-title">
    <div class="container">
      <header class="section-head" data-reveal>
        <p class="eyebrow">Diensten</p>
        <h2 id="diensten-title">Vijf diensten, elk met een vaste prijs</h2>
        <p class="section-head__intro">
          Gevelwerk, binnenafwerking, microcement, beton ciré en latex spuitwerk – elk uitgevoerd
          op vaste prijs en binnen de afgesproken bouwtermijn. Vraag per dienst een aparte
          offerte aan.
        </p>
      </header>
      <div class="diensten" data-reveal-group>
        {
          diensten.map((dienst) => (
            <a href={dienst.href} class="dienst-card">
              <div class={`dienst-card__media dienst-card__media--${dienst.foto}`}>
                <Picture
                  src={dienst.src}
                  widths={cardWidths}
                  sizes={cardSizes}
                  formats={['avif', 'webp']}
                  alt={dienst.alt}
                  loading="lazy"
                  decoding="async"
                  data-demo="true"
                />
              </div>
              <div class="dienst-card__body">
                <h3>{dienst.titel}</h3>
                <p>{dienst.belofte}</p>
                <span class="dienst-card__more">
                  Meer informatie
                  <span class="dienst-card__arrow" aria-hidden="true">&rarr;</span>
                </span>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <!-- 5. Projectbewijs / voor-na galerij -->
  <section class="section section--alt" aria-labelledby="galerij-title">
    <div class="container">
      <header class="section-head" data-reveal>
        <p class="eyebrow">Projectbewijs</p>
        <h2 id="galerij-title">Het werk, voor en na</h2>
        <p class="section-head__intro">
          Bekijk afgeronde projecten in Haarlem, Bloemendaal en Zandvoort. Voor-na foto's van elk
          type werk dat Pietracasa uitvoert.
        </p>
      </header>
      <div class="galerij" data-reveal-group>
        {
          galerij.map((project) => (
            <figure class="ba-card" data-ba>
              <div class="ba-card__media">
                <div class="ba-card__panel is-active" data-ba-panel="na">
                  <Picture
                    src={project.na.src}
                    widths={cardWidths}
                    sizes={cardSizes}
                    formats={['avif', 'webp']}
                    alt={project.na.alt}
                    loading="lazy"
                    decoding="async"
                    data-demo="true"
                  />
                </div>
                <div class="ba-card__panel" data-ba-panel="voor" aria-hidden="true">
                  <Picture
                    src={project.voor.src}
                    widths={cardWidths}
                    sizes={cardSizes}
                    formats={['avif', 'webp']}
                    alt={project.voor.alt}
                    loading="lazy"
                    decoding="async"
                    data-demo="true"
                  />
                </div>
              </div>
              <div class="ba-card__bar">
                <figcaption>{project.label}</figcaption>
                <div
                  class="ba-toggle"
                  role="group"
                  aria-label={`Voor- of na-foto tonen van ${project.label}`}
                >
                  <button type="button" data-ba-show="voor" aria-pressed="false">Voor</button>
                  <button type="button" data-ba-show="na" aria-pressed="true">Na</button>
                </div>
              </div>
              <div class="ba-card__case">
                <h3>{project.titel}</h3>
                <ul class="case-meta" role="list">
                  {project.meta.map((m) => <li>{m}</li>)}
                </ul>
              </div>
            </figure>
          ))
        }
      </div>
      <div class="galerij__footer" data-reveal>
        <a href="/projecten/" class="btn btn--secondary">Bekijk alle zes uitgelichte projecten</a>
      </div>
    </div>
  </section>

  <!-- 6. Werkwijze -->
  <section class="section" aria-labelledby="werkwijze-title">
    <div class="container">
      <header class="section-head" data-reveal>
        <p class="eyebrow">Werkwijze</p>
        <h2 id="werkwijze-title">Zo werkt het: vijf vaste stappen</h2>
        <p class="section-head__intro">
          Van eerste contact tot oplevering verloopt elk project via vijf vaste stappen – zonder
          verrassingen in planning of prijs. U ontvangt per stap een schriftelijke bevestiging.
        </p>
      </header>
      <ol class="steps" role="list" data-reveal-group>
        {
          stappen.map((stap, i) => (
            <li class="step">
              <span class="step__medaillon" aria-hidden="true">
                <Image
                  src={stapFotos[i]}
                  alt=""
                  width={192}
                  height={192}
                  loading="lazy"
                  decoding="async"
                  data-demo="true"
                />
              </span>
              <div class="step__top">
                <span class="step__nr tabular" aria-hidden="true">{stap.nr}</span>
                <span class="step__icon" set:html={icons[stap.icon]} aria-hidden="true" />
              </div>
              <h3>{stap.titel}</h3>
              <p>{stap.tekst}</p>
            </li>
          ))
        }
      </ol>
    </div>
  </section>

  <!-- 7. Garanties & keurmerken -->
  <section class="section section--alt garanties-sectie" aria-labelledby="garanties-title">
    <div class="garanties-sectie__bg" aria-hidden="true">
      <Picture
          data-parallax
          data-parallax-speed="6"
        src={bgTroffelSweep}
        widths={[768, 1280, 1536]}
        sizes="100vw"
        formats={['avif', 'webp']}
        alt=""
        loading="lazy"
        decoding="async"
        data-demo="true"
      />
    </div>
    <div class="container garanties-sectie__inner">
      <header class="section-head" data-reveal>
        <p class="eyebrow">Garanties</p>
        <h2 id="garanties-title">Afspraken die op papier staan</h2>
        <p class="section-head__intro">
          Pietracasa levert met vaste prijsgarantie en een schriftelijke opleveringsafspraak.
        </p>
      </header>
      <ul class="garanties" role="list" data-reveal-group>
        {
          garanties.map((garantie) => (
            <li class="garantie">
              <span class="garantie__icon" set:html={icons[garantie.icon]} aria-hidden="true" />
              <h3>{garantie.titel}</h3>
              <p>{garantie.tekst}</p>
            </li>
          ))
        }
      </ul>
      <p class="garanties__note" data-reveal>
        Keurmerken en branchevereniging-lidmaatschappen tonen wij hier zodra ze geverifieerd zijn.
      </p>
    </div>
  </section>

  <!-- 8. Reviews (kaarten = DEMO met AI-portretten; vervang door echte reviews) -->
  <section class="section" aria-labelledby="reviews-title">
    <div class="container">
      <header class="section-head reviews-head" data-reveal>
        <div>
          <p class="eyebrow">Reviews</p>
          <h2 id="reviews-title">Beoordeeld met 4,8 sterren op Google</h2>
          <p class="section-head__intro">
            25+ geverifieerde reviews van particulieren en bouwbedrijven uit Haarlem en omgeving
            – over planning, prijs en afwerking.
          </p>
        </div>
        <div class="review-score" aria-hidden="true">
          <p class="review-score__num tabular" data-count-to="4.8" data-count-decimals="1">4,8</p>
          <p class="review-score__stars">
            <span set:html={icons.star} />
            <span set:html={icons.star} />
            <span set:html={icons.star} />
            <span set:html={icons.star} />
            <span set:html={icons.starPartial} />
          </p>
        </div>
      </header>
      <div class="stats__proof" data-reveal>
        <ul class="stats__fotos" role="list" aria-hidden="true">
          {[detailGevel, detailWand, detailVloer].map((foto) => (
            <li>
              <Image src={foto} alt="" width={144} height={144} loading="lazy" decoding="async" data-demo="true" />
            </li>
          ))}
        </ul>
        <p class="stats__caption">
          Beoordeeld door bouwbedrijven en huiseigenaren in de regio Haarlem. Alle projecten op
          tijd en in budget afgeleverd.
        </p>
      </div>
      <ul class="review-kaarten" role="list" data-reveal-group>
        {
          demoReviews.map((review) => (
            <li class="review-kaart" data-demo="true">
              <p class="review-kaart__stars" aria-hidden="true">
                <span set:html={icons.star} />
                <span set:html={icons.star} />
                <span set:html={icons.star} />
                <span set:html={icons.star} />
                <span set:html={icons.star} />
              </p>
              <blockquote>
                <p>&ldquo;{review.quote}&rdquo;</p>
              </blockquote>
              <footer class="review-kaart__wie">
                <span class="review-kaart__foto">
                  <Image
                    src={review.foto}
                    alt=""
                    width={96}
                    height={96}
                    loading="lazy"
                    decoding="async"
                    data-demo="true"
                  />
                </span>
                <span>
                  <strong>{review.naam}</strong>
                  <small>{review.plaats}</small>
                </span>
              </footer>
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <!-- 9. CTA-band -->
  <section class="cta-band" aria-labelledby="cta-title" data-motes="dark">
    <div class="cta-band__bg" aria-hidden="true">
      <Picture
        src={ctaSfeer}
        widths={[820, 1280, 1920]}
        sizes="100vw"
        formats={['avif', 'webp']}
        alt=""
        loading="lazy"
        decoding="async"
        data-demo="true"
      />
    </div>
    <div class="sheen" aria-hidden="true"></div>
    <div class="container cta-band__inner" data-reveal>
      <h2 id="cta-title">Vraag vrijblijvend een offerte aan</h2>
      <p class="cta-band__sub">
        U ontvangt binnen 48 uur een reactie met een vaste prijsopgave. Geen verrassingen
        achteraf.
      </p>
      <div class="cta-band__actions">
        <a href="/offerte-aanvragen/" class="btn btn--primary">
          Offerte aanvragen
          <span class="btn__arrow" aria-hidden="true">&rarr;</span>
        </a>
        <a href="tel:0681943404" class="cta-band__tel">Liever direct contact? Bel 06 81 94 34 04</a>
      </div>
      <p class="cta-band__micro">
        Vrijblijvend – vaste prijs, reactie binnen 48 uur. Bereikbaar ma–vr 7:00–17:00.
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  /* --- Gedeelde sectie-koppen --- */
  .eyebrow {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-text-secondary);
  }

  .section-head {
    max-width: 68ch;
    margin-block-end: clamp(var(--spacing-xl), 6vw, var(--spacing-2xl));
  }

  .section-head h2 {
    margin-block-start: var(--spacing-md);
  }

  .section-head__intro {
    margin-block-start: var(--spacing-md);
    color: var(--color-text-secondary);
  }

  .micro {
    margin-block-start: var(--spacing-md);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  /* --- 1. Hero --- */
  .hero {
    padding-block: clamp(var(--spacing-lg), 4vw, var(--spacing-2xl))
      clamp(var(--spacing-2xl), 8vw, var(--spacing-section));
  }

  .hero__grid {
    display: grid;
    gap: var(--spacing-lg);
    align-items: center;
  }

  .hero__media {
    position: relative;
    aspect-ratio: 16 / 10;
    overflow: hidden;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
  }

  /* Voor-na sleep-slider in de hero (zelfde mechaniek als de galerij-kaarten) */
  .hero__ba {
    position: absolute;
    inset: 0;
  }

  .hero__ba-media {
    position: absolute !important;
    inset: 0;
    aspect-ratio: auto !important;
  }

  .hero__ba :global(picture) {
    position: absolute;
    inset: 0;
    display: block;
  }

  .hero__media :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    animation: hero-settle 1200ms cubic-bezier(0.22, 1, 0.36, 1) 250ms both;
  }

  /* Signature: gladstrijkende laag trekt van links naar rechts over het beeld */
  .hero__media::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 2;
    background: linear-gradient(100deg, var(--color-neutral-100), var(--color-neutral-300));
    transform-origin: right center;
    animation: hero-sweep 1000ms cubic-bezier(0.22, 1, 0.36, 1) 250ms both;
  }

  @keyframes hero-sweep {
    from {
      transform: scaleX(1);
    }
    to {
      transform: scaleX(0);
    }
  }

  @keyframes hero-settle {
    from {
      transform: scale(1.06);
    }
    to {
      transform: scale(1);
    }
  }

  .hero__chip {
    position: absolute;
    z-index: 1;
    inset-block-end: var(--spacing-md);
    inset-inline-start: var(--spacing-md);
    padding: var(--spacing-xs) var(--spacing-md);
    border-radius: var(--radius-pill);
    background: color-mix(in srgb, var(--color-neutral-900) 70%, transparent);
    color: var(--color-text-inverse);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
  }

  @media (min-width: 768px) {
    .br-mobiel {
      display: none;
    }
  }

  .hero__sub {
    text-wrap: pretty;
    margin-block-start: var(--spacing-lg);
    font-size: clamp(var(--font-size-base), 2.5vw, var(--font-size-md));
    color: var(--color-text-secondary);
    max-width: 54ch;
  }

  .hero__content h1 {
    margin-block-start: var(--spacing-md);
  }

      .hero__proof {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-sm);
    margin-block-start: var(--spacing-lg);
  }

  .hero__proof li {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-sm);
    width: min(100%, 320px);
    padding: 12px var(--spacing-lg);
    background: var(--color-surface-default);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-md);
    box-shadow: 0 2px 10px rgb(33 27 21 / 0.05);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    white-space: nowrap;
  }

  @media (min-width: 768px) {
    .hero__proof {
      flex-direction: row;
      align-items: stretch;
      justify-content: flex-start;
    }

    .hero__proof li {
      width: auto;
    }
  }

  .hero__proof-star {
    display: inline-flex;
    color: var(--color-primary-default);
  }

  .hero__proof-star :global(svg) {
    width: 14px;
    height: 14px;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-md);
    margin-block-start: var(--spacing-xl);
  }

  .hero__actions .btn {
    width: 100%;
  }

  @media (min-width: 640px) {
    .hero__actions .btn {
      width: auto;
    }
  }

  @media (min-width: 1024px) {
    .hero__grid {
      grid-template-columns: 1.06fr 0.94fr;
      gap: var(--spacing-2xl);
    }

    .hero__media {
      aspect-ratio: auto;
      align-self: stretch;
      min-height: 100%;
    }

    .hero__content {
      padding-block: var(--spacing-2xl);
    }
  }

  /* --- 2. Bewijs-band --- */
  .stats-band {
    padding-block: clamp(var(--spacing-xl), 6vw, var(--spacing-2xl));
    border-block: 1px solid var(--color-border-default);
  }

  .stats {
    display: grid;
    gap: var(--spacing-lg);
    margin: 0;
  }

  .stats__item {
    display: flex;
    flex-direction: column-reverse;
    gap: var(--spacing-xs);
    padding: var(--spacing-lg);
    background: var(--color-surface-default);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
  }

  .stats__item:first-child {
    border-top: 0;
    padding-block-start: 0;
  }

  .stats__item dt {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-text-secondary);
  }

  .stats__item dd {
    margin: 0;
    display: flex;
    align-items: baseline;
    gap: var(--spacing-sm);
  }

  .stats__num {
    font-family: var(--font-display);
    font-size: clamp(var(--font-size-xl), 6vw, var(--font-size-display));
    font-weight: var(--font-weight-bold);
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .stats__star {
    display: inline-flex;
    color: var(--color-primary-default);
  }

  .stats__star :global(svg) {
    width: 22px;
    height: 22px;
  }

  /* Werk-detail trio (demo-beelden) als visueel bewijs naast de caption */
  .stats__proof {
    margin-block-end: var(--spacing-xl);
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--spacing-sm) var(--spacing-md);
    margin-block-start: var(--spacing-lg);
  }

  .stats__fotos {
    display: flex;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .stats__fotos li {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid var(--color-background-default);
    box-shadow: 0 4px 12px rgb(33 27 21 / 0.12);
  }

  .stats__fotos li + li {
    margin-inline-start: -14px;
  }

  .stats__fotos :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .stats__caption {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    max-width: 56ch;
    margin: 0;
    flex: 1;
    min-width: 28ch;
  }

  @media (min-width: 768px) {
    .stats {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--spacing-xl);
    }

    .stats__item {
      border-top: 0;
      padding-block-start: 0;
      border-inline-start: 1px solid var(--color-border-default);
      padding-inline-start: var(--spacing-xl);
    }

    .stats__item:first-child {
      border-inline-start: 0;
      padding-inline-start: 0;
    }
  }

  /* --- 3. Probleem naar oplossing --- */
  .contrast {
    display: grid;
    gap: var(--spacing-lg);
  }

  .contrast__col h3 {
    margin-block-end: var(--spacing-md);
  }

  .contrast__col ul {
    list-style: none;
    padding: 0;
  }

  .contrast__col li {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-md);
    padding-block: var(--spacing-md);
    border-top: 1px solid var(--color-border-default);
  }

  .contrast__col {
    background: var(--color-surface-alt);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-lg);
    padding: var(--spacing-lg);
  }

  .contrast__col--oplossing {
    background: var(--color-surface-default);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    padding: var(--spacing-lg);
  }

  .mark {
    flex: 0 0 auto;
    width: 28px;
    height: 28px;
    display: grid;
    place-items: center;
    border-radius: var(--radius-sm);
    margin-block-start: 2px;
  }

  .mark :global(svg) {
    width: 15px;
    height: 15px;
  }

  .mark--x {
    background: var(--color-neutral-200);
    color: var(--color-neutral-700);
  }

  .mark--check {
    background: var(--color-primary-default);
    color: var(--color-primary-on);
  }

  @media (min-width: 900px) {
    .contrast {
      grid-template-columns: 1fr 1.08fr;
      gap: var(--spacing-2xl);
      align-items: start;
    }
  }

  /* --- 4. Diensten --- */
  .diensten {
    display: grid;
    gap: var(--spacing-lg);
  }

  .dienst-card {
    display: block;
    background: var(--color-surface-default);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
    color: inherit;
    text-decoration: none;
    transition:
      transform 200ms ease,
      box-shadow 200ms ease;
  }

  .dienst-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
    color: inherit;
  }

  .dienst-card:focus-visible {
    outline: 3px solid var(--color-border-focus);
    outline-offset: 2px;
  }

  .dienst-card__media {
    aspect-ratio: 3 / 2;
    overflow: hidden;
  }

  .dienst-card__media :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform-origin: 50% 30%;
    transition: transform 400ms var(--motion-easing);
  }

  /* Crop-anker = gezichtspositie van de vakman in elk demo-beeld:
     bij elke cover-crop of hover-zoom blijft het hoofd in beeld */
  .dienst-card__media--gevelwerk :global(img) {
    object-position: 49% 30%;
  }

  .dienst-card__media--binnen :global(img) {
    object-position: 38% 24%;
  }

  .dienst-card__media--vloer :global(img) {
    object-position: 54% 21%;
  }

  .dienst-card:hover .dienst-card__media :global(img) {
    transform: scale(1.05);
  }

  .dienst-card__body {
    padding: var(--spacing-lg);
  }

  .dienst-card__body p {
    margin-block-start: var(--spacing-sm);
    color: var(--color-text-secondary);
  }

  .dienst-card__more {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-sm);
    margin-block-start: var(--spacing-md);
    font-weight: var(--font-weight-semibold);
    color: var(--color-primary-default);
  }

  .dienst-card__arrow {
    transition: transform 180ms ease;
  }

  .dienst-card:hover .dienst-card__arrow {
    transform: translateX(4px);
  }

  @media (min-width: 768px) {
    .diensten {
      grid-template-columns: repeat(6, 1fr);
    }

    /* 5 kaarten: rij van 3, rij van 2 gecentreerd */
    .dienst-card {
      grid-column: span 2;
    }

    .dienst-card:nth-child(4) {
      grid-column: 2 / span 2;
    }

    .dienst-card:nth-child(5) {
      grid-column: 4 / span 2;
    }
  }

  /* --- 5. Voor-na galerij --- */
  .galerij {
    display: grid;
    gap: var(--spacing-lg);
  }

  .ba-card {
    margin: 0;
    background: var(--color-surface-default);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
  }

  .ba-card__media {
    position: relative;
    aspect-ratio: 4 / 3;
  }

  .ba-card__panel {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: opacity var(--motion-base) var(--motion-easing);
  }

  .ba-card__panel.is-active {
    opacity: 1;
  }

  .ba-card__panel :global(picture) {
    position: absolute;
    inset: 0;
    display: block;
  }

  .ba-card__panel :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .ba-card__bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-sm);
    padding: var(--spacing-sm) var(--spacing-md);
  }

  .ba-card__bar figcaption {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
  }

  .ba-toggle {
    display: inline-flex;
    border: 1px solid var(--color-border-strong);
    border-radius: var(--radius-pill);
    padding: var(--spacing-xs);
    gap: var(--spacing-xs);
  }

  .ba-toggle button {
    min-height: 36px;
    min-width: 56px;
    padding: var(--spacing-xs) var(--spacing-md);
    border: 0;
    border-radius: var(--radius-pill);
    background: transparent;
    font-family: var(--font-body);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-secondary);
    cursor: pointer;
    transition:
      background-color var(--motion-fast) var(--motion-easing),
      color var(--motion-fast) var(--motion-easing);
  }

  .ba-toggle button:hover {
    color: var(--color-text-primary);
  }

  .ba-toggle button:focus-visible {
    outline: 3px solid var(--color-border-focus);
    outline-offset: 2px;
  }

  .ba-toggle button[aria-pressed='true'] {
    background: var(--color-neutral-900);
    color: var(--color-text-inverse);
  }

  .galerij__footer {
    margin-block-start: var(--spacing-xl);
  }

  @media (min-width: 768px) {
    .galerij {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  /* --- 6. Werkwijze --- */
  .steps {
    list-style: none;
    padding: 0;
    display: grid;
    gap: var(--spacing-xl);
  }

  .step {
    background: var(--color-surface-default);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    padding: var(--spacing-lg);
  }

  /* Foto-medaillon per stap (demo-beeld) */
  .step__medaillon {
    display: block;
    width: 84px;
    height: 84px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid var(--color-surface-default);
    box-shadow: 0 6px 18px rgb(33 27 21 / 0.14);
    margin-block-end: var(--spacing-sm);
  }

  .step__medaillon :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .step__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-md);
  }

  .step__nr {
    font-family: var(--font-display);
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-bold);
    color: var(--color-primary-default);
    line-height: 1;
  }

  .step__icon {
    display: inline-flex;
    color: var(--color-text-secondary);
  }

  .step__icon :global(svg) {
    width: 24px;
    height: 24px;
  }

  .step h3 {
    margin-block-start: var(--spacing-md);
    font-size: var(--font-size-md);
  }

  .step p {
    margin-block-start: var(--spacing-sm);
    color: var(--color-text-secondary);
  }

  @media (min-width: 768px) {
    .steps {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .steps {
      grid-template-columns: repeat(5, 1fr);
      gap: var(--spacing-lg);
    }
  }

  /* --- 7. Garanties --- */
  /* Troffel-sweep als sectie-accent rechtsboven (demo-beeld bg-troffel-sweep).
     Alleen vanaf 768px: op mobiel vult de tekst de volle breedte, daar is geen
     lege hoek en zou de textuur onder de tekst komen. */
  .garanties-sectie {
    position: relative;
    overflow: hidden;
  }

  .garanties-sectie__bg {
    display: none;
    position: absolute;
    inset: 0;
  }

  .garanties-sectie__bg :global(picture) {
    position: absolute;
    inset: 0;
    display: block;
  }

  .garanties-sectie__bg :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.45;
  }

  /* Counter-scrim: dekt de textuur volledig af waar tekst staat; alleen de lege
     hoek rechts van de sectiekop blijft zichtbaar, dus contrast blijft gelijk */
  .garanties-sectie__bg::after {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(50% 50% at 100% 0%, transparent, var(--color-surface-alt) 68%);
  }

  @media (min-width: 768px) {
    .garanties-sectie__bg {
      display: block;
    }
  }

  .garanties-sectie__inner {
    position: relative;
  }

  .garanties {
    list-style: none;
    padding: 0;
    display: grid;
    gap: var(--spacing-lg);
  }

  .garantie {
    background: var(--color-surface-default);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    padding: var(--spacing-lg);
  }

  .garantie__icon {
    display: grid;
    place-items: center;
    width: 44px;
    height: 44px;
    border-radius: var(--radius-md);
    background: color-mix(in srgb, var(--color-primary-default) 10%, transparent);
    color: var(--color-primary-default);
  }

  .garantie__icon :global(svg) {
    width: 22px;
    height: 22px;
  }

  .garantie h3 {
    margin-block-start: var(--spacing-md);
    font-size: var(--font-size-base);
  }

  .garantie p {
    margin-block-start: var(--spacing-sm);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  .garanties__note {
    margin-block-start: var(--spacing-lg);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
  }

  @media (min-width: 640px) {
    .garanties {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .garanties {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  /* --- 8. Reviews --- */
  .reviews {
    display: grid;
    gap: var(--spacing-xl);
  }

  .review-score {
    background: var(--color-neutral-900);
    color: var(--color-text-inverse);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-lg);
    padding: clamp(var(--spacing-lg), 5vw, var(--spacing-2xl));
  }

  .review-score__num {
    font-family: var(--font-display);
    font-size: clamp(var(--font-size-display), 10vw, 80px);
    font-weight: var(--font-weight-bold);
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .review-score__stars {
    display: flex;
    gap: var(--spacing-xs);
    margin-block-start: var(--spacing-md);
    color: var(--color-text-inverse);
  }

  .review-score__stars :global(svg) {
    width: 22px;
    height: 22px;
  }

  .review-score__sub {
    margin-block-start: var(--spacing-md);
    font-size: var(--font-size-sm);
    color: var(--color-neutral-300);
    max-width: 28ch;
  }

  .reviews__text h2 {
    margin-block-start: var(--spacing-md);
  }

  .reviews__text p:not(.eyebrow) {
    margin-block-start: var(--spacing-md);
    color: var(--color-text-secondary);
  }

  @media (min-width: 900px) {
    .reviews {
      grid-template-columns: 0.8fr 1.2fr;
      gap: var(--spacing-2xl);
      align-items: center;
    }
  }

  /* --- 9. CTA-band --- */
  .cta-band {
    position: relative;
    overflow: hidden;
    padding-block: clamp(var(--spacing-section), 14vw, 160px);
    color: var(--color-text-inverse);
  }

  .cta-band__bg {
    position: absolute;
    inset: 0;
  }

  .cta-band__bg :global(picture) {
    position: absolute;
    inset: 0;
    display: block;
  }

  .cta-band__bg :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* AA-contrast: op mobiel loopt de tekst tot de rechterrand — daar blijft de scrim dekkend */
  .cta-band__bg::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
      100deg,
      color-mix(in srgb, var(--color-neutral-900) 86%, transparent),
      color-mix(in srgb, var(--color-neutral-900) 72%, transparent)
    );
  }

  @media (min-width: 768px) {
    .cta-band__bg::after {
      background: linear-gradient(
        100deg,
        color-mix(in srgb, var(--color-neutral-900) 86%, transparent),
        color-mix(in srgb, var(--color-neutral-900) 60%, transparent)
      );
    }
  }

  .cta-band__inner {
    position: relative;
    z-index: 1;
  }

  .cta-band__sub {
    margin-block-start: var(--spacing-md);
    font-size: var(--font-size-md);
    color: var(--color-neutral-100);
    max-width: 46ch;
  }

  .cta-band__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--spacing-lg);
    margin-block-start: var(--spacing-xl);
  }

  .cta-band__tel {
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    color: var(--color-text-inverse);
    font-weight: var(--font-weight-semibold);
    text-decoration: underline;
    text-underline-offset: 0.2em;
  }

  .cta-band__tel:hover {
    color: var(--color-neutral-200);
  }

  .cta-band__micro {
    margin-block-start: var(--spacing-md);
    font-size: var(--font-size-sm);
    color: var(--color-neutral-100);
  }

  /* --- Review-kaarten (DEMO) --- */
  .reviews-head {
    display: flex;
    flex-wrap: wrap;
    align-items: end;
    justify-content: space-between;
    gap: var(--spacing-lg);
    max-width: none;
  }

  .review-kaarten {
    display: grid;
    gap: var(--spacing-lg);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .review-kaart {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
    background: var(--color-surface-default);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-lg);
    padding: clamp(var(--spacing-lg), 3vw, var(--spacing-xl));
  }

  .review-kaart__stars {
    display: flex;
    gap: 2px;
    color: var(--color-primary-default);
  }

  .review-kaart__stars :global(svg) {
    width: 16px;
    height: 16px;
  }

  .review-kaart blockquote {
    margin: 0;
    flex: 1;
  }

  .review-kaart blockquote p {
    color: var(--color-text-default);
  }

  .review-kaart__wie {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
  }

  .review-kaart__foto {
    flex: none;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    overflow: hidden;
  }

  .review-kaart__foto :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .review-kaart__wie strong {
    display: block;
  }

  .review-kaart__wie small {
    color: var(--color-text-secondary);
  }

  @media (min-width: 900px) {
    .review-kaarten {
      grid-template-columns: repeat(3, 1fr);
    }
  }


  /* --- Case-kop onder de home-galerijkaarten --- */
  .ba-card__case {
    padding: var(--spacing-md) var(--spacing-lg) var(--spacing-lg);
    border-top: 1px solid var(--color-border-default);
  }

  .ba-card__case h3 {
    font-size: var(--font-size-md);
  }

  .case-meta {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-xs);
    margin: var(--spacing-sm) 0 0;
    padding: 0;
    list-style: none;
  }

  .case-meta li {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-semibold);
    color: var(--color-primary-default);
    background: color-mix(in srgb, var(--color-primary-default) 10%, transparent);
    border-radius: var(--radius-pill);
    padding: 2px 10px;
  }

</style>

<script>
  // Voor-na toggles in de projectgalerij.
  document.querySelectorAll<HTMLElement>('[data-ba]').forEach((card) => {
    const buttons = Array.from(card.querySelectorAll<HTMLButtonElement>('[data-ba-show]'));
    const panels = Array.from(card.querySelectorAll<HTMLElement>('[data-ba-panel]'));
    buttons.forEach((btn) => {
      btn.addEventListener('click', () => {
        const target = btn.dataset.baShow;
        buttons.forEach((b) => b.setAttribute('aria-pressed', String(b === btn)));
        panels.forEach((panel) => {
          const active = panel.dataset.baPanel === target;
          panel.classList.toggle('is-active', active);
          panel.setAttribute('aria-hidden', String(!active));
        });
      });
    });
  });
</script>
