---
import '@fontsource/inter/400.css';
import '@fontsource/inter/500.css';
import '@fontsource/inter/600.css';
import '@fontsource/space-grotesk/500.css';
import '@fontsource/space-grotesk/600.css';
import '@fontsource/space-grotesk/700.css';
import interWoff2 from '@fontsource/inter/files/inter-latin-400-normal.woff2?url';
import groteskWoff2 from '@fontsource/space-grotesk/files/space-grotesk-latin-700-normal.woff2?url';
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  noindex?: boolean;
}

const { title, description, ogImage, noindex = false } = Astro.props;

const site = Astro.site ?? new URL('https://pietracasa.netlify.app');
const canonical = new URL(Astro.url.pathname, site);
const toonActiebalk = !Astro.url.pathname.startsWith('/offerte-aanvragen');
const ogImageUrl = ogImage
  ? new URL(ogImage, site).href
  : new URL('/og-pietracasa.jpg', site).href;
const plausibleDomain = site.hostname;

const localBusiness = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  name: 'Pietracasa',
  description:
    'Stucadoor in Haarlem en omgeving. Gevelwerk, binnenafwerking en microcement vloeren op vaste prijs.',
  url: site.href,
  telephone: '+31681943404',
  email: 'info@pietracasa.nl',
  address: {
    '@type': 'PostalAddress',
    addressRegion: 'Noord-Holland',
    addressCountry: 'NL',
  },
  areaServed: ['Haarlem', 'Bloemendaal', 'Zandvoort'],
  openingHoursSpecification: [
    {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'],
      opens: '07:00',
      closes: '17:00',
    },
  ],
};
---

<!doctype html>
<html lang="nl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex" />}
    <link rel="canonical" href={canonical.href} />
    <link rel="icon" type="image/png" href="/favicon-pietracasa.png" />

    <link rel="preload" as="font" type="font/woff2" href={interWoff2} crossorigin />
    <link rel="preload" as="font" type="font/woff2" href={groteskWoff2} crossorigin />

    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical.href} />
    {ogImageUrl && <meta property="og:image" content={ogImageUrl} />}
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    {ogImageUrl && <meta name="twitter:image" content={ogImageUrl} />}

    <script type="application/ld+json" set:html={JSON.stringify(localBusiness)} is:inline />

    <slot name="head" />

    <script is:inline>
      // Markeer dat JS draait, vóór first paint — reveals verbergen content
      // alleen onder html.js (zie global.css).
      document.documentElement.classList.add('js');
    </script>

    {
      import.meta.env.PROD && (
        <script
          defer
          data-domain={plausibleDomain}
          src="https://plausible.io/js/script.js"
          is:inline
        />
      )
    }
  </head>
  <body>
    <a href="#main" class="skip-link">Ga naar inhoud</a>
    <Header />
    <main id="main">
      <slot />
    </main>
    <Footer />
    {
      toonActiebalk && (
        <nav class="actiebalk" aria-label="Snel contact">
          <a href="tel:0681943404" class="actiebalk__bel">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 4h4l2 5-2.5 1.5a12 12 0 0 0 5 5L15 13l5 2v4a2 2 0 0 1-2 2A16 16 0 0 1 3 6a2 2 0 0 1 2-2z"/></svg>
            Bellen
          </a>
          <a href="/offerte-aanvragen/" class="actiebalk__offerte">
            Offerte aanvragen
            <span aria-hidden="true">&rarr;</span>
          </a>
        </nav>
      )
    }
    <script>
      import { initMotion } from '../scripts/motion';
      initMotion();
    </script>
  </body>
</html>
