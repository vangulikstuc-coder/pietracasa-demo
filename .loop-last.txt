De commit hoef ik niet zelf te doen: `build.sh` regel 17 commit na elke beurt automatisch (`git add -A && git commit -qm "beurt N (auto)"`) — vandaar de bestaande "(auto)"-commits. De git-schrijfacties zijn in deze sandbox geblokkeerd, dus ik laat de wrapper dat afronden. De taak zelf is klaar en geverifieerd.

## Beurt 12 — page_contact afgerond

**Gebouwd:** `site/src/pages/contact.astro` met alle 7 blueprint-secties, plus een nieuwe Cloudflare Pages Function `site/functions/api/contact.ts` voor het formulier.

- **Paginaheader** (aardetint): titel + dominant klikbaar telefoonnummer 06 81 94 34 04 in display-formaat.
- **Twee paden**: bel-kaart en mail-kaart met iconen, micro-garantie direct eronder (AC: zichtbaar bij contactkaart).
- **Contactformulier**: naam, telefoon, e-mail, omschrijving — zelfde validatie/honeypot/no-JS-fallback-patroon als /offerte/, micro-garantie onder de verzendknop. De function logt en bevestigt; lead-aflevering staat nog open in open-questions.md.
- **Werkgebied**: schematische SVG-radiuskaart (15 km, Haarlem/Bloemendaal/Zandvoort) met disclaimer, hergebruikt van de over-pagina.
- **Openingstijden**: tabel per dag (ma–vr 7:00–17:00, weekend gesloten — afgeleid uit contact.json) met gemarkeerde spoed-noot.
- **Vertrouwensband** (donker): 4,8★ Google · 50+ projecten sinds 2020 · vaste prijs op schrift.
- **CTA-band** (vlak aardetint, bewust geen sfeerbeeld i.v.m. "geen full-bleed sfeeroverload"): primair "Offerte aanvragen", secundair "Bekijk afgeronde projecten".

**Geverifieerd:** `pnpm build` slaagt (11 pagina's). Geen TODO/lorem, geen verboden zinsneden, geen jij-vorm, geen hardcoded hex, 1× h1, 7× klikbare tel-link, 3× micro-garantie. Het enige uitroepteken is "Bedankt!" uit de verplichte microcopy. 3× `btn--primary` in dist (submit, CTA-band, drawer) — gelijk aan alle andere pagina's, in gescheiden viewports. De bestaande header/footer-links naar /contact/ werken nu.

**Administratie:** `page_contact` staat op `passes: true` in prd.json, beurtverslag staat in progress.md. Resterende taken: **demo-beelden** en **qa**.
