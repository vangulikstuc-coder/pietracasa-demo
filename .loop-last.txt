Commit-rechten zijn in deze sessie geblokkeerd — en dat verklaart ook de git-historie: elke eerdere commit heet "beurt N (auto)", dus de loop-wrapper commit zelf na afloop van de beurt (vandaar ook de `.loop-last.txt`-mutatie). Het werk staat klaar in de working tree; de auto-commit pakt het op.

**Beurt 11 afgerond: taak `page_faq` is gebouwd en op `passes: true` gezet.**

Wat er staat:

- **`site/src/pages/faq.astro`** — drie blueprint-secties:
  1. **Tekst-hero** (werkwijze-patroon met rustige primary-gloed): H1 "Antwoord op uw vragen over kosten, planning en werkwijze", primaire CTA + telefoonlink + micro-garantie boven de vouw.
  2. **FAQ gegroepeerd, bezwaren eerst** — 4 thema's (kosten en vaste prijs → planning en doorlooptijd → werkgebied en diensten → garantie en afspraken) met in totaal 14 vragen als native `<details name="…">`-accordion: exclusief-open per groep, 44px touch targets, focus-visible outline, roterend plus-icoon. Sticky intro-kolom met thema-ankerlinks op desktop.
  3. **CTA-band "Nog een vraag?"** met cta-sfeerbeeld + scrim, primaire CTA, klikbare telefoon- en maillink. Bewust géén link naar `/contact/` — die pagina bestaat nog niet (volgende taak).
- **FAQPage JSON-LD** (14 Questions) in de `<head>` via de head-slot, naast de bestaande LocalBusiness-data.
- **Feiten-discipline**: alleen bevestigde feiten in de antwoorden (vaste prijs, 48u reactie, 50+ projecten sinds 2020, 4,8★/25+ reviews, werkgebied tot 15 km, KvK-geregistreerd en verzekerd). Geen doorlooptijden of tarieven verzonnen — "verschilt per type werk; standaardtermijnen noemen wij niet".
- **Verificatie**: `pnpm build` slaagt (10 pagina's). Checks schoon: geen verboden zinsneden, geen jij-vorm, geen uitroeptekens in copy, geen TODO/lorem, geen hardcoded hex, 1× h1, 2× micro-garantie, 3× `btn--primary` in dist (gelijk aan alle andere pagina's). Screenshots blijven in deze sandbox niet mogelijk (genoteerd voor de qa-beurt); responsive geverifieerd via statische CSS-review van de hergebruikte, eerder geteste patronen.
- **Administratie**: `prd.json` → `page_faq: passes: true`; beurt-log + aanwijzingen voor de volgende beurt (page_contact) in `progress.md`.

Volgende beurt: **page_contact** — formulier-patroon hergebruiken uit `offerte.astro` + Pages Function, en daarna de faq-CTA-band eventueel naar `/contact/` laten linken.
