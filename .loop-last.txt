De lus commit zelf: `build.sh` regel 17 draait na elke beurt `git add -A && git commit -m "beurt N (auto)"`. Mijn handmatige commit is dus niet nodig (en de sandbox blokkeert hem toch) — alle wijzigingen worden automatisch gecommit zodra deze beurt eindigt.

**Beurt 4 afgerond: de Offerte-pagina (taak `page_offerte`) is gebouwd en op `passes: true` gezet.**

Wat er is gemaakt:

- **`site/src/pages/offerte.astro`** — alle 9 secties uit de blueprint: pagina-kop met drie garantie-chips (vrijblijvend / vaste prijs / 48u), bewijs-strip vóór het formulier (4,8★ · 25+ reviews · 50+ projecten), formulier-card met sticky garantieblok ernaast, "Of bel direct"-band met groot klikbaar telefoonnummer, dienst-icoonkaarten, 4-stappen traject (aanvraag → afspraak op locatie → vaste-prijs offerte → uitvoering & oplevering), twee voor-na fotoduo's met Voor/Na-chips, review-scoreblok zonder verzonnen citaten, en een donkere CTA-band die terug ankert naar het formulier.
- **`site/functions/api/offerte.ts`** — Cloudflare Pages Function die het formulier afhandelt: honeypot-spamfilter, JSON-antwoord voor het fetch-pad, en een 303-redirect naar `#aanvraag-ontvangen`/`#aanvraag-mislukt` als no-JS-fallback (die panelen worden via CSS `:target` zichtbaar, dus het formulier werkt ook zonder JavaScript).
- Formulier heeft exact de zes blueprint-velden, validatie/laad/succes/fout-teksten letterlijk uit `microcopy.md`, foutmeldingen via `aria-describedby` met focus naar het eerste foutveld, en de micro-garantie bij elke CTA.

Verificatie: `pnpm build` slaagt (3 pagina's), geen TODO/PLACEHOLDER/lorem, geen verboden zinsneden, consequent "u", precies één primaire CTA per viewport, alle demo-beelden via astro:assets met `data-demo="true"`.

Twee bewuste keuzes vastgelegd in `progress.md` en `open-questions.md`: formulierlabels zijn neutraal-formeel ("Naam" i.p.v. het jij-vormige "Jouw naam" uit de microcopy-bibliotheek, omdat de pagina-AC "uitsluitend u" vereist), en e-mail/CRM-aflevering van aanvragen plus de Google-profiel-URL staan als open vragen — die feiten zijn niet bevestigd, dus niets verzonnen. Volgende beurt pakt `page_projecten` op.
