Bevestigd: `build.sh` (regel 17) commit zelf na elke beurt met `git add -A && git commit -qm "beurt N (auto)"` — daarom vereisen mijn git-commando's hier goedkeuring en is zelf committen niet nodig. De lus-administratie is verder compleet.

## Beurt 8 afgerond — Microcement-pagina

**Wat is gebouwd:** `site/src/pages/microcement.astro` volgens het 4-secties-blueprint:

1. **Pagina-hero** — full-bleed resultaat-shot (`hero-microcement.jpg`, preload + fetchpriority high), sweep-signature, primaire CTA "Offerte aanvragen" + belnummer + micro-garantie boven de vouw.
2. **Kern-inhoud** — genummerde toepassingen-lijst (naadloze vloeren, badkamers, renovatie over bestaande ondergrond) naast `dienst-microcement.jpg`.
3. **Kosten & doorlooptijd** — vier Q&A-icoonkaarten, omdat het blueprint expliciet vroeg om veelgestelde vragen over kosten en doorlooptijd. Géén bedragen of doorlooptijden verzonnen: vaste prijs na inspectie op locatie is het enige prijspad, planning staat in de offerte.
4. **Bewijs + CTA** — voor-na toggle met het vloer-paar, feitenlijst (4,8 sterren, 50+ projecten, KvK & verzekerd), secundaire link naar /projecten/, en de CTA-band met sfeerbeeld.

**Verificatie:** `pnpm build` slaagt (7 pagina's). Checks schoon: geen TODO/lorem, geen verboden zinsneden, consequent u-vorm, geen uitroeptekens, 1× h1, alle 5 beelden via astro:assets met `data-demo="true"`. Het aantal primaire CTA's in de dist-HTML (3, incl. mobiele drawer) is identiek aan alle bestaande pagina's.

**Administratie:** `page_microcement` staat op `passes: true` in prd.json; progress.md heeft notities voor de volgende beurt (page_werkwijze — geen eigen hero-beeld beschikbaar, tekstkop aanbevolen). De commit volgt automatisch via build.sh zodra deze beurt eindigt.
