De commit hoeft niet handmatig: het buitenste loop-script (`build.sh`, regel 17) doet na elke beurt zelf `git add -A && git commit -m "beurt N (auto)"` — vandaar dat alle eerdere commits ook "(auto)" heten en mijn git-aanroepen geblokkeerd worden. Het werk staat klaar om automatisch gecommit te worden.

**Beurt 9 afgerond — taak `page_werkwijze` is klaar.**

Wat er is gebouwd in `site/src/pages/werkwijze.astro`:
- **Tekst-hero** met rustige primary-gloed en primaire CTA + micro-garantie boven de vouw. Er is geen werkwijze-beeld in `public/images/` en de huisregels staan een heldere tekstkop toe; `over-werkomgeving.jpg` blijft gereserveerd voor de over-pagina.
- **5-stappen-tijdlijn** (opname → offerte → planning → uitvoering → oplevering) met verbindingslijn, icoon-badges en per stap een "Afspraak"-pill (reactie binnen 48 uur, vaste prijs op schrift, data op papier, meerwerk alleen na akkoord, oplevering op datum). Intro-kolom is sticky op desktop.
- **Bewijsblok**: "vast op papier"-lijst met hairline-dividers + het donkere review-scoreblok (4,8 sterren, 25+ reviews) + secundaire link naar /projecten/.
- **CTA-band** met sfeerbeeld + scrim, telefoonlink en micro-garantie.

Verificatie: `pnpm build` slaagt (8 pagina's), 1× h1, geen verboden zinsneden/jij-vorm/uitroeptekens/TODO, uitsluitend semantische tokens, geen verzonnen doorlooptijden of feiten, CTA-telling gelijk aan de andere pagina's. Screenshots blijven in deze sandbox onmogelijk (geen Playwright/Chrome-MCP) — statische responsive review gedaan, visuele check volgt in de qa-beurt. `prd.json` staat op `passes: true` en `progress.md` is bijgewerkt met aanwijzingen voor de volgende beurt (page_over-stucq: twee beelden kopiëren, werkgebied-AC).
