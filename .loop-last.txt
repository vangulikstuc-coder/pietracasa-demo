De commit-stap wordt door de bouw-lus zelf afgehandeld: `build.sh` regel 17 doet na elke beurt `git add -A && git commit -qm "beurt N (auto)"` (vandaar de "beurt 12 (auto)"-commits in de history). Git staat niet in de allowlist van deze sandbox, dus ik laat het committen bewust aan het loop-script over — de wijzigingen staan klaar in de working tree.

**Beurt 13 afgerond — taak `demo-beelden` staat op `passes: true`.**

Wat deze beurt opleverde:

- **Volledige slot-audit tegen `public/images/README.md`**: alle 18 demo-beelden zijn gekoppeld op de bedoelde plekken — 6 hero-splits op de juiste pagina's, het dienst-trio op home/projecten/dienstpagina's, de 3 losse voor/na-paren in de toggle-galerijen (niet de samengestelde splits), drone-still op projecten + gevelwerk, werkomgevingsfoto op de over-pagina en cta-sfeer in de CTA-banden.
- **Dist-audit via Node-script over alle 11 gebouwde pagina's**: 38 content-afbeeldingen, allemaal als `<picture>` met AVIF + WebP via astro:assets, expliciete width/height, `data-demo="true"` op elk beeld, hero's met `fetchpriority="high"` (niet lazy) en alle overige beelden `loading="lazy" decoding="async"` — nul problemen.
- **Randgevallen gecontroleerd**: `heroHome` in offerte.astro is alleen een OG-image-fallback (toegestaan per asset-spec), er zijn geen kale `/images/`-verwijzingen in de src, en de DEMO-waarschuwing in de README is onaangetast.
- Er waren **geen codewijzigingen nodig** — de koppeling was tijdens de paginabeurten al correct opgebouwd; deze beurt heeft dat integraal geverifieerd en afgevinkt. `pnpm build` slaagt.

Volgende beurt pakt de laatste taak op: **qa** (eindcontrole hele site — CTA-telling per pagina, Lighthouse, [CONFIRM:]-markers verzamelen, prefers-reduced-motion).
